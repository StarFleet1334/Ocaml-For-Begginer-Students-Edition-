
(**
  
Operators as functions
( + ) if we run this in utop we will see that it takes
int -> int and returns int

( + ) 3 4 run this utop and you will see 7

*)

let add = ( + ) 3

(** THen run add ? any numbers in utop and you can play with that
    THis is one of the operators as functions    

    you can do (+) or ( + ) space in partentheses
    but in case of multiplication, you can not 
    trim spaces.
*)

let mul = ( * ) 4